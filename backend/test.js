const mongoose = require('mongoose'); const dotenv = require('dotenv'); dotenv.config({ path: './.env' }); mongoose.connect(process.env.MONGO_URI || 'mongodb://localhost:27017/zatca-erp'); const run = async () => { const Invoice = require('./models/Invoice.js').default; const inerrs = await Invoice.find({ invoiceNumber: { $regex: '^BAKALA-' } }).select('invoiceNumber'); console.log(inerrs.map(i => i.invoiceNumber)); process.exit(); }; run();
